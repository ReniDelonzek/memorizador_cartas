import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('VerRankingPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VerRankingPage(title: 'VerRanking')));
    //  final titleFinder = find.text('VerRanking');
    //  expect(titleFinder, findsOneWidget);
  });
}
