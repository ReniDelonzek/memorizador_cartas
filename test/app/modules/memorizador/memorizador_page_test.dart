import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MemorizadorPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MemorizadorPage(title: 'Memorizador')));
    //  final titleFinder = find.text('Memorizador');
    //  expect(titleFinder, findsOneWidget);
  });
}
